{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "rm -rf .next/cache && npm run build",
  "crons": [
    {
      "path": "/api/sync/residents",
      "schedule": "0 * * * *"
    }
  ]
}
